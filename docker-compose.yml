services:
  trust-crawl:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: trust-crawl
    restart: unless-stopped
    ports:
      - "6970:6970"
    environment:
      PORT: "6970"
      SOCKET_GRACE_MS: "60000"