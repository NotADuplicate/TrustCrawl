services:
  trust-crawl:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: trust-crawl
    restart: unless-stopped
    ports:
      - "6970:4000"
    environment:
      PORT: "4000"
      SOCKET_GRACE_MS: "60000"